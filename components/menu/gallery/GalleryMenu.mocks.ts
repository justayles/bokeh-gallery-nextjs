import { galleryLinks } from 'lib/menu/links';
import { IGalleryMenuProps } from './GalleryMenu';

const base: IGalleryMenuProps = {
  links: galleryLinks,
};

export const mockGalleryMenuProps = {
  base,
};
