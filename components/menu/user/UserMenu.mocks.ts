import { userDropdownLinks } from 'lib/menu/links';
import { UserMenuProps } from './UserMenu';

const base: UserMenuProps = { links: userDropdownLinks };

export const mockUserMenuProps = {
  base,
};
